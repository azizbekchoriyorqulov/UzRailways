server:
  port: 8080

spring:
  application:
    name: UzRailways
  datasource:
    username: postgres
    password: 1111
    url: jdbc:postgresql://localhost:5432/rw
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL92Dialect
